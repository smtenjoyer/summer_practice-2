# Имя рабочего процесса, отображаемое на GitHub Actions
name: Build and Test Qt Project on Linux (Aqtinstall Final Fix)

# События, которые запускают этот рабочий процесс
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

# Определяем задачи (jobs)
jobs:
  build_linux:
    runs-on: ubuntu-latest # Запускаем на стандартной машине GitHub Actions
    
    steps:
    - name: Checkout Repository # Шаг 1: Клонирование вашего репозитория
      uses: actions/checkout@v4

    - name: Install Build Essentials and Dependencies # Шаг 2: Установка базовых инструментов (без Qt)
      run: |
        echo "Updating apt packages..."
        sudo apt update -y
        echo "Installing build essentials and other core libraries..."
        # build-essential для g++, make. libgl1-mesa-dev для OpenGL.
        # Python3 и pip нужны для aqtinstall.
        sudo apt install -y build-essential libgl1-mesa-dev python3 python3-pip

    - name: Install Qt using aqtinstall # Шаг 3: Установка Qt с помощью aqtinstall
      run: |
        echo "Installing aqtinstall..."
        pip3 install aqtinstall

        echo "Installing Qt 5.15.2 for linux/gcc_64..."
        # КОРРЕКТНЫЙ ПОРЯДОК АРГУМЕНТОВ: ОС, ТИП, ВЕРСИЯ.
        aqt install-qt linux desktop 5.15.2 -m qtbase --outputdir "$RUNNER_TEMP/Qt"

        # Добавляем путь к Qt в PATH и LD_LIBRARY_PATH
        echo "$RUNNER_TEMP/Qt/5.15.2/gcc_64/bin" >> $GITHUB_PATH
        echo "$RUNNER_TEMP/Qt/5.15.2/gcc_64/lib" >> $LD_LIBRARY_PATH
        export LD_LIBRARY_PATH="$RUNNER_TEMP/Qt/5.15.2/gcc_64/lib:$LD_LIBRARY_PATH"

        # Выведем PATH и LD_LIBRARY_PATH для отладки
        echo "PATH: $PATH"
        echo "LD_LIBRARY_PATH: $LD_LIBRARY_PATH"

    - name: Configure Qt Projects # Шаг 4: Настройка проектов с помощью qmake
      run: |
        echo "Configuring server project..."
        cd server
        qmake server.pro
        cd ..

        echo "Configuring client project..."
        cd client
        qmake client.pro
        cd ..

    - name: Build Server # Шаг 5: Сборка серверного приложения
      run: |
        echo "Building server..."
        cd server
        make -j$(nproc)
        mv server ../server_app
        cd ..

    - name: Build Client # Шаг 6: Сборка клиентского приложения
      run: |
        echo "Building client..."
        cd client
        make -j$(nproc)
        mv client ../client_app
        cd ..

    - name: Verify Executables # Шаг 7: Проверка наличия исполняемых файлов
      run: |
        echo "Verifying compiled executables..."
        ls -l
        if [ -f server_app ]; then
          echo "Server application (server_app) found."
        else
          echo "Server application (server_app) NOT found!"
          exit 1
        fi
        if [ -f client_app ]; then
          echo "Client application (client_app) found."
        else
          echo "Client application (client_app) NOT found!"
          exit 1
        fi

    # - name: Run Basic Test (Optional)
    #   run: |
    #     echo "Attempting to run server and client..."
    #     ./server_app &
    #     SERVER_PID=$!
    #     sleep 2
    #     ./client_app "testuser" || true
    #     sleep 1
    #     kill $SERVER_PID || true
    #     echo "Server stopped."

    - name: Upload Build Artifacts (Optional) # Шаг 8 (Опционально): Сохранение скомпилированных файлов как артефактов
      uses: actions/upload-artifact@v4
      with:
        name: Qt_Apps_Linux_Build
        path: |
          ./client_app
          ./client_app.pdb # If debugging symbols are generated for Windows, add them.
          ./server_app
          ./server_app.pdb # If debugging symbols are generated for Windows, add them.